"use strict";
/*
$(document).ready(function(){
    
  var $covers = $(".cover");
  
  var pageIsSmall = (window.innerWidth <= 991)? true : false;
  
  if(pageIsSmall){
    $covers.clone().addClass("smallCover").removeClass("cover").appendTo("#first-row");
    $covers.hide();
  }
   
  $(window).resize(function(){
    
    if(window.innerWidth <= 991){
      if(!pageIsSmall){
        pageIsSmall = true;
        $covers.clone().addClass("smallCover").removeClass("cover").appendTo("#first-row");
        $covers.hide();
      }
    } else {
      if(pageIsSmall){
        pageIsSmall = false;
        $(".smallCover").remove();
        $(".cover").show();
      }
    }
  });
});
*/
